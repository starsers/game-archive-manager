<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="game_archive_manager.ContentDialogs.GameConfig"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:game_archive_manager.ContentDialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:game_archive_manager.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    x:Name="RootPage">

    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <!-- Content body -->

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="游戏名称：" x:Name="GameNameTextBlock" Margin="8,0,0,0" VerticalAlignment="Center" LineHeight="20" />
            <TextBox PlaceholderText="在此输入..." Width="300" />
        </StackPanel>
        <controls:ImageUploader />
        <!--<StackPanel Orientation="Horizontal">
            <TextBlock Text="匹配规则：" Margin="8,0,0,0" VerticalAlignment="Center" LineHeight="20" />
            <ComboBox Width="300" PlaceholderText="请选择" ItemsSource="{Binding GetMatchRuleNames}"/>
        </StackPanel>-->
        <ItemsControl ItemsSource="{Binding ActiveMatchRules}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding RuleName}" Width="200" Margin="8,0,0,0" VerticalAlignment="Center" />
                        <ComboBox Width="30" PlaceholderText="请选择" 
                                  ItemsSource="{Binding OtherMatchRuleNames}" 
                                  DisplayMemberPath="Rule.RuleName" 
                                  SelectedItem="{Binding SelectedRule, Mode=TwoWay}"  
                                  SelectionChanged="Rules_SelectionChanged" />
                        <Button Content="删除" Command="{Binding ElementName=RootPage, Path=RemoveRuleCommand}" CommandParameter="{Binding}" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Content="添加规则" Command="{Binding AddRuleCommand}" Margin="0,8,0,0" />

        <CheckBox Content="Upload your content to the cloud."/>
    </StackPanel>

</Page>
