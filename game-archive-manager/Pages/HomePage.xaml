<?xml version="1.0" encoding="utf-8"?>
<Page
   x:Class="game_archive_manager.HomePage"
   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
   xmlns:local="using:game_archive_manager"
   xmlns:controls="using:game_archive_manager.Controls"
   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
   xmlns:data="using:game_archive_manager"
   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
   mc:Ignorable="d"
   Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <RelativePanel VerticalAlignment="Top" HorizontalAlignment="Right">
           <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="Add Game"  Click="AddGame_Button_Click" Margin="10" />
                <AutoSuggestBox x:Name="SearchBox"
                   PlaceholderText="Search for game"
                   TextChanged="Search_Control_TextChanged"
                   QueryIcon="Find"
                   QuerySubmitted="Search_Control_QuerySubmitted"
                   SuggestionChosen="Search_Control_SuggestionChosen"
                   Margin="10"/>
               <Button x:Name="loginButton" Content="Login out" Click="ShowSignInDialogButton_Click" Margin="10"></Button>
               <Button Content="Settings"  Click="NavigateToSettingsPage" Margin="10" />
           </StackPanel>
       </RelativePanel>

       <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
           <!--<TextBlock Text="Home Page" HorizontalAlignment="Center" VerticalAlignment="Center" />
           <FlipView MaxWidth="400" Height="180" BorderBrush="Black" BorderThickness="1" ItemsSource="{x:Bind Items, Mode=OneWay}">
               <FlipView.ItemTemplate>
                   <DataTemplate x:DataType="data:ControlInfoDataItem">
                       <Grid>
                           <Image Height="120" Source="{x:Bind ImagePath}" Stretch="Uniform" VerticalAlignment="Top"/>
                           <Border Background="#A5FFFFFF" Height="60" VerticalAlignment="Bottom">
                               <TextBlock Text="{x:Bind Title}" Foreground="{ThemeResource TextFillColorPrimary}" Padding="12,12" Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center"/>
                           </Border>
                       </Grid>
                   </DataTemplate>
               </FlipView.ItemTemplate>
           </FlipView>-->
       </StackPanel>
       <controls:ContentShow x:Name="GamesContentShow" Margin="20"/>
       <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom">
           
       </StackPanel>
   </Grid>
</Page>