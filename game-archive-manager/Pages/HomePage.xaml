<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="game_archive_manager.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:game_archive_manager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <RelativePanel VerticalAlignment="Top" HorizontalAlignment="Right">

            <Button x:Name="loginButton" RelativePanel.AlignRightWithPanel="True" Content="Open Sign In Dialog" Click="ShowSignInDialogButton_Click" Margin="10"></Button>
            <Button Content="Settings" RelativePanel.LeftOf="loginButton" Click="NavigateToSettingsPage" Margin="10" />

        </RelativePanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Home Page" HorizontalAlignment="Center" VerticalAlignment="Center" />
            <!--<FlipView MaxWidth="400" Height="180" BorderBrush="Black" BorderThickness="1" ItemsSource="{x:Bind Items, Mode=OneWay}">
                <FlipView.ItemTemplate>
                    <DataTemplate x:DataType="data:ControlInfoDataItem">
                        <Grid>
                            <Image Height="120" Source="{x:Bind ImagePath}" Stretch="Uniform" VerticalAlignment="Top"/>
                            <Border Background="#A5FFFFFF" Height="60" VerticalAlignment="Bottom">
                                <TextBlock Text="{x:Bind Title}" Foreground="#CCFFFFFF" Padding="12,12" Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </FlipView.ItemTemplate>
            </FlipView>-->
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom">

        </StackPanel>
    </Grid>


</Page>